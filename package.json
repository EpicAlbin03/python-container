{
  "name": "python-container",
  "private": true,
  "scripts": {
    "tailwind": "npx @tailwindcss/cli -i ./input.css -o ./studentplatform/static/css/style.css --watch",
    "docker:up": "docker compose up --watch && bun run post-build",
    "docker:detached": "docker compose up -d && bun run post-build",
    "docker:build": "docker compose up --build --watch && bun run post-build",
    "post-build": "bun run ./post-build.sh",
    "docker:down": "docker compose down",
    "docker:remove": "docker compose down -v"
  },
  "dependencies": {},
  "devDependencies": {
    "tailwindcss": "^4.2.2",
    "tw-animate-css": "^1.4.0"
  }
}
